name: tests
on:
  pull_request:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
      run:
        shell: bash


    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      - name: realpath
        run: realpath ./

      - name: dir
        run: dir

      - name: dir
        run: dir ../

      - name: ls
        run: ls

      - name: run ubuntu
        run: cat ./dove_download.sh



