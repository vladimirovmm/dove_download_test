name: My action
description: Download dove
inputs:
  version:
    description: Dove version
    required: true
    default: "latest"
runs:
  using: "composite"
  steps:
    - run: ls -l ${{ github.action_path }}
      shell: bash
    - run: ls -l
      shell: bash
    - run: ./dove_download.sh ${{ inputs.version }}
      shell: bash